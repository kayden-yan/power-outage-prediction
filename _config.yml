remote_theme: pages-themes/cayman@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one
title: 'Power Outage Discovery'
description: 'How do we identify patterns in power outages in order to better prevent them?'
